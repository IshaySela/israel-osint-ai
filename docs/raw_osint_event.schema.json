{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RawOsintEvent",
  "description": "Base class for all unprocessed osint events",
  "type": "object",
  "properties": {
    "app_ev_id": {
      "type": "string",
      "format": "uuid",
      "description": "UUID generated at the scraper level"
    },
    "raw_message": {
      "type": "string",
      "description": "The full raw message text from the source"
    },
    "timestamp": {
      "type": "string"
    },
    "source": {
      "type": "string"
    },
    "data": {
      "type": "object",
      "description": "Source-specific fields. Shape depends on source."
    }
  },
  "required": ["app_ev_id", "raw_message", "timestamp", "source", "data"]
}
